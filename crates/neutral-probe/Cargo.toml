[package]
name = "neutral-probe"
description = "Reader-only inspection library and standalone probe for Neutral artifacts"
readme = "README.md"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[package.metadata.neutral]
owner = "independent-probe"

[dependencies]
neutral-core = { path = "../neutral-core" }
neutral-encoding = { path = "../neutral-encoding" }
neutral-reader = { path = "../neutral-reader" }

[dev-dependencies]
neutral-ir = { path = "../neutral-ir" }

[lints]
workspace = true
