<!-- SPDX-License-Identifier: Apache-2.0 -->

# Released versions

Each child directory is an immutable executable conformance bundle named by
the exact package release version. Bundles coexist so maintenance and
compatibility tests never depend on whichever future plan is active in
`portable/`.
